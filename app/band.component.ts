import { Component } from '@angular/core';

import { Band } from './band';
import { BandsService } from './bands.service';

@Component({
  template: `<h1>{{title}}</h1>`,
  // styleUrls:
})

export class BandComponent  { title = 'The rolling'; }
